# NAME

encode.sh - Encode data using OpenSSL AES-256-CBC encryption

# SYNOPSIS

`encode.sh` [data_to_encode]

# DESCRIPTION

`encode.sh` is a simple script that encodes data using OpenSSL AES-256-CBC encryption. It takes a single argument, the data to be encoded, and encrypts it using the specified encryption algorithm.

# EXAMPLE

```bash
./encode.sh "data_to_encode"
