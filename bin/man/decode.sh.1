# NAME

decode.sh - Decode a string using OpenSSL AES-256-CBC encryption

# SYNOPSIS

`decode.sh` [string_to_decode]

# DESCRIPTION

`decode.sh` is a simple script that decodes a string using OpenSSL AES-256-CBC encryption. It takes a single argument, the string to be decoded, and decrypts it using the specified encryption algorithm.

# EXAMPLE

```bash
./decode.sh "encoded_string"

